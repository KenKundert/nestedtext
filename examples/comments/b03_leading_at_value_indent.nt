server:
# a leading comment at indent 0; the next data line is 'port' at indent 4,
# and rule 1 (Leading) accepts indent <= the key's indent, so this is
# a leading comment for 'server.port'
    port: 8080
    host: 0.0.0.0
